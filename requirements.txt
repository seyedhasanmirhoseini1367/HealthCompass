# NOTE: versions are unpinned for flexibility during active development.
# Before go-live, run: pip install pip-tools && pip-compile requirements.txt
# This produces requirements.lock with exact hashes for reproducible builds.
django>=5.2
pillow
python-decouple
google-genai
groq
onnxruntime
defusedxml
django-storages[s3]
boto3
rank-bm25
openai
scikit-learn
pdfplumber
pandas
numpy
anthropic
pyarrow
joblib
scipy
langgraph==0.2.28
# Google OAuth — allow any 65.x but not 66+ which may have breaking changes
django-allauth>=65.0.0,<66.0.0
PyJWT
requests
# Mobile API
djangorestframework
djangorestframework-simplejwt
django-cors-headers
firebase-admin
# Security: field-level encryption + rate limiting
# cryptography package provides Fernet (AES-128-CBC + HMAC-SHA256) for
# EncryptedCharField in apps/accounts/fields.py.
# It is already a transitive dependency of several packages (google-genai,
# anthropic, etc.) — listed here explicitly to guarantee it is present.
cryptography
django-ratelimit
# Cache backend for django-ratelimit (multi-worker safe) and population insights.
# Django 4.0+ built-in Redis backend uses this package directly.
# Set CACHE_URL=redis://... in env to activate; falls back to locmem in dev.
redis
# Production server & database
gunicorn
psycopg2-binary
whitenoise
dj-database-url
